# Bot para whatsapp com a biblioteca Venom

## Instalar Dependências
- npm init -y
- npm i venom-bot

Adicione um arquivo pdf dentro do diretório principal e configure o caminho do arquivo dentro da função client.sendFile()
